name = "wb-tv-tracker-bot"
main = "index.mjs"
compatibility_date = "2025-01-01"
account_id = "21bb5b4f01c1715e57a750b0f28f6a94"

# Deployed worker URL:
#   https://wb-tv-tracker-bot.valet-bd8bb6.workers.dev/webhook
#
# Secrets (set via wrangler secret put — NOT in this file):
#   TELEGRAM_BOT_TOKEN
#
# The worker fetches REPORT.json / MODELS.json / ANOMALIES.json from
# the repository's raw GitHub URL — no API token needed because the
# repo is read-public via raw.githubusercontent.com.

[vars]
